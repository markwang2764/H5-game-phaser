export enum DataSystemType {
    USER,
}

export enum EnterType {
    WX = 1,
    HB,
    APP
}

export enum GameMode {
    PK = 1,
    /**闯关模式 */
    LEVEL
}

export enum PkResult {
    LOST,
    WIN
}